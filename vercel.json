{
    "builds": [
      { "src": "dashboard/package.json", "use": "@vercel/next" },
      { "src": "core/server.ts", "use": "@vercel/node" }
    ],
    "routes": [
      { "src": "/api/(.*)", "dest": "/core/server.ts" },
      { "src": "/(.*)", "dest": "/dashboard/$1" }
    ]
  }
  